# Relay Server

RELAY_ENV=development
PORT=8080
DASHBOARD_URL=http://localhost:3000

# Admin API (generate with: openssl rand -base64 32)
ADMIN_API_KEY=

# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/relay?sslmode=disable
DB_MAX_CONNS=10
DB_MIN_CONNS=2

# Redis
REDIS_URL=redis://localhost:6379/0
# Stream value is required by config, but server routes per workspace.
REDIS_STREAM=agent-stream:org-123:workspace-456
REDIS_CONSUMER_GROUP=agent-group
REDIS_DLQ_STREAM=relay-events-dlq
TRACE_HEADER_NAME=X-Trace-Id

# WorkOS
WORKOS_API_KEY=
WORKOS_CLIENT_ID=
WORKOS_REDIRECT_URI=http://localhost:8080/auth/callback

# Webhooks
WEBHOOK_BASE_URL=http://localhost:8080

# ArangoDB
ARANGO_URL=http://localhost:8529
ARANGO_USERNAME=root
ARANGO_PASSWORD=
ARANGO_DATABASE=codegraph

# OpenTelemetry (optional)
# OTEL_EXPORTER_OTLP_ENDPOINT=
# OTEL_EXPORTER_OTLP_HEADERS=
OTEL_SERVICE_NAME=relay-server
OTEL_SERVICE_VERSION=dev
