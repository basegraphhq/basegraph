[phases.setup]
# AgentOS v0 toolbelt for Railway/Nixpacks runtime.
# Needed for repo setup/sync and for ExploreTools (rg).
aptPkgs = [
  "ca-certificates",
  "git",
  "make",
  "openssh-client",
  "ripgrep",
  "tree",
]
